# 社区运营手册（内部）

TensorHub 社区注册表运营规范。原则：**内容在贡献者自己的仓库，我们只管理元数据与链接**；以「通知-删除」方式响应举报，不主动承担内容审查义务。

## 角色与权限

- **维护者（Maintainers）**：有权合并 registry.json 的 PR、回复 Issue、设置 `verified` 标记、下架条目。至少 2 人，互为备份。
- **贡献者（Contributors）**：通过 Issue 表单提交，无权直接修改 registry.json。

## 收录流程（对应 CONTRIBUTING.md）

1. 贡献者填写 Issue 表单（`submission` label 自动打上）。
2. 维护者评估（≤ 2 个工作日）：
   - 跑本地校验：`python3 scripts/validate_registry.py`（网络检查需要 GITHUB_TOKEN）。
   - 人工检查仓库：README 可读、SKILL.md 结构完整、无恶意模式。
   - 通过 → 维护者建分支加条目 → 提交 PR（触发 validate.yml 自动校验）→ 人工复核 → 合并。
   - 拒绝 → 在 Issue 中说明原因并关闭。
3. 合并后自动触发 stats.yml 拉取该条目的 GitHub 指标。

## 审核标准

### 安全（一票否决）

- SKILL.md / 脚本中存在可疑模式：`curl ... | sh`、`base64 -d` 后执行、下载远程文件到启动目录、读取 `.env`/密钥文件并外传。
- MCP 启动命令指向不可信来源（非官方 registry、非知名发布渠道）。
- 仓库内含硬编码密钥（可用 `gitleaks` 快速扫描，可选步骤）。

### 质量（不满足可打回修改）

- SKILL.md 有明确用途与使用步骤；README 可读。
- 仓库有许可证文件，`license` 字段与之一致。
- 描述准确，不含夸大宣传。

### 合规（一票否决）

- 内容违反中国法律法规（黄赌毒、政治敏感、诈骗、翻墙类）或 GitHub 社区准则。
- 收集用户隐私且无明确告知。

## 下架流程（通知-删除）

1. 收到举报（Issue 标题带「举报」或邮件）。
2. **24 小时内**评估：确认违规 → 直接从 `registry.json` 移除条目并合并（无需等 PR 流程），同时：
   - 在移除 PR 的 commit message 注明原因与举报来源；
   - 在对应 Issue 或邮件回复举报人处理结果；
   - 通知条目作者（下架原因 + 申诉渠道）。
3. 恶意条目（含恶意代码、已确认的诈骗）加入 `SECURITY.md` 黑名单段落，永久禁止再次提交。
4. 申诉：作者可在 7 天内申诉；确属误判则恢复条目。

## 热度与统计（自动化，无需人工）

| 数据 | 来源 | 更新频率 |
|---|---|---|
| stars / forks / 下载量 | GitHub API（stats.yml） | 每日 02:23 UTC + registry 变更时 |
| 安装量 / 使用量 | App 匿名打点 → Cloudflare Worker | 实时入 KV，聚合即查 |

热度只用于排序展示，不参与审核。禁止人工刷量；发现异常刷量（同仓库安装量突增 10 倍以上）可临时从热榜隐藏该条目，观察 7 天。

## 举报与联系渠道

- 举报入口：仓库 Issues（标题前缀「举报」）
- 联系邮箱：`<维护者邮箱>`（在 README 中公开）

## 免责声明（已在 CONTRIBUTING.md 公开）

本注册表仅提供元数据与链接，不托管、不审查、不担保第三方仓库内容；按通知-删除原则运营。
