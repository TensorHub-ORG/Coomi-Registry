# 提交指南（Contributing）

欢迎向 Coomi 社区注册表提交你的 SKILL / MCP / Workflow。本仓库只存储**元数据和链接**，所有内容都托管在贡献者自己的公开 GitHub 仓库中——你的仓库始终归你所有，社区只负责收录和展示。

## 提交流程

1. **填写 Issue 表单**：点击仓库页面的「Issues → New Issue → 提交 SKILL / MCP / Workflow」，
   按表单填写名称、仓库地址、类型、描述、许可证等。
2. **维护者评估**：TensorHub 维护者将在 1~2 个工作日内评估你的提交。
3. **合入注册表**：评估通过后，维护者将条目加入 `registry.json` 并提交 PR。
   PR 触发 `validate.yml` 自动校验（JSON 格式、仓库可达、SKILL.md 存在、许可证等）。
4. **发布**：合入 `main` 后，Coomi App「市场」页约 10 分钟内可见你的条目。

> 提交者不需要 fork 本仓库或手写 PR——Issue 表单提交后由维护者代为合入，以保证
> 每次变更都经过人工审核 + 自动化校验两道关卡。

## 收录要求

| 项目 | 要求 |
|---|---|
| 仓库 | 必须公开，且在 `registry.json` 中可被访问 |
| 许可证 | 仓库内必须有 LICENSE 文件，`license` 字段填写具体的许可证名称 |
| SKILL | `SKILL.md` 必须存在于仓库根目录或你填写的 `subdir` 目录中 |
| 安全性 | 不得包含恶意代码、硬编码密钥（API Key 等）、收集用户隐私的代码 |
| 合规性 | 内容不得违反中国法律法规或 GitHub 社区准则 |

## 条目字段说明（registry.json）

```json
{
  "id": "my-skill",                // 唯一 ID，^[a-z0-9][a-z0-9-]*$，合入时由维护者确定
  "name": "My Skill",              // 展示名称
  "description": "English description. 中文描述。",  // 中英双语，空格分隔
  "repository": "owner/repo",      // 仓库（owner/repo 或完整 URL）
  "ref": "main",                   // 分支名
  "subdir": "",                    // 技能所在子目录，留空表示仓库根目录
  "author": "github-username",     // 提交者
  "tags": ["android", "debug"],    // 检索标签
  "license": "MIT",                // 开源许可证
  "verified": false,               // 仅维护者审核后置为 true
  "submitted_at": "2026-08-12"     // 提交日期
}
```

`description` 字段要求**中英双语**（英文在前、中文在后，用空格或句号分隔），App 市场页直接展示该字段。

`mcps` 与 `workflows` 条目使用同样的基础字段（Workflow 暂为收录展示，安装支持将在后续版本提供）。

## 审核标准（维护者）

- **安全**：检查 SKILL.md 与脚本是否存在可疑命令（curl | sh、base64 解码执行、
  隐藏的凭据读取等）；MCP 检查启动命令是否指向可信来源。
- **质量**：SKILL.md 结构完整（名称、用途、使用步骤），README 可读。
- **合规**：内容违反法律法规或平台准则的一律拒绝，并在 24 小时内下架已有条目。

## 下架与举报

如果你发现某个条目存在安全问题、恶意代码或违规内容，请：

1. 通过仓库 Issues 提交举报（标题注明「举报」），或发送邮件至维护者。
2. 维护者将在 **24 小时内**评估并决定是否从 `registry.json` 移除该条目。
3. 下架后会通知条目作者；涉及恶意行为的条目会被加入黑名单，禁止再次提交。

## 免责声明

- 社区注册表仅提供元数据与仓库链接，内容由各仓库作者独立维护，TensorHub 不对
  第三方仓库的内容、质量与安全性作任何担保。
- 安装第三方 SKILL / MCP 前，请自行审阅其源码与许可证。
- 本注册表按「通知-删除」原则运营：收到有效举报后及时下架相关条目。
